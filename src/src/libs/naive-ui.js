/**
 * Enable type hints
 * @type import('naive-ui').GlobalThemeOverrides
 */
const themeOverrides = {
  common: {
    fontFamily: "'Poppins', sans-serif",
    fontWeightStrong: 600,
    primaryColor: "#00ccff",
    primaryColorHover: "#8ae8ff",
    primaryColorPressed: "#68dffc",
  },
};

export default themeOverrides;
